name: Weekly Multi-Stock Price Update

on:
  # Run every Monday at 8 AM UTC
  schedule:
    - cron: '0 8 * * 1'
  
  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Configure Git
        run: |
          git config --global user.email "docravikumar@hotmail.com"
          git config --global user.name "gnrh1"
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      
      - name: Run multi-stock pipeline
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
        run: |
          python run_pipeline.py
      
      - name: Push changes
        run: |
          git push
        continue-on-error: false
      
      - name: Notify on failure
        if: failure()
        run: |
          echo "::error::Weekly price update pipeline failed"
          echo "::error::Check the logs above for details"
          exit 1
